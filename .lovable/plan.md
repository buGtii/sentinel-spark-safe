# CyberSmart Pro-Grade Upgrade Plan\n\nThis plan groups the 15 requested areas into 6 deliverable phases. Each phase is self-contained so the app stays usable between phases.\n\n## Phase 1 — False-Positive Fix & Layered Trust Scoring (highest priority)\n\n**Backend (`supabase/functions/scan-url/index.ts`)**\n\n- Rebuild verdict pipeline as 8 independent signal layers, each producing `{score 0–100, weight, evidence[]}`:\n  1. Heuristics (current logic — weight reduced)\n  2. VirusTotal (authoritative when present)\n  3. AI reasoning (Lovable AI `google/gemini-2.5-flash`)\n  4. SSL / HTTPS validity (cert fetch + issuer check)\n  5. Domain age via WHOIS (rdap.org — no key required)\n  6. Business-legitimacy heuristics (favicon present, og tags, contact page reachable)\n  7. Brand-similarity / homograph (Levenshtein vs. top-50 brands)\n  8. Behavioural indicators (redirect chain, suspicious params)\n- **Weighted blend** instead of \